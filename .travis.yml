language: node_js
node_js:
  - "lts/dubnium"

install:
  - npm install

script:
  - npm run compile
  - npm run test
  - npm install
  - cd samples
  - npm install
  - npm test

deploy:
  provider: npm
  email: "${env.CI_EMAIL}"
  api_key: "${env.CI_API_KEY}"
  on:
    tags: true
